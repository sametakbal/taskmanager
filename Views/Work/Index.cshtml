@model IEnumerable<taskmanager.Models.Work>
<div class="text-center">
<div id="view-all">
    @await Html.PartialAsync("_ViewAll",Model) 
       @if(Model.Count() == 0){
        <h4>No work has been entered yet</h4>
    }
</div>
</div>